# Como executar o projeto?

Na classe **ExtractJsonMockaroo** no diretório test, alterar a váriavel host para sua URL e API Key da sua conta do Mockaroo

Projeto Java, efetuar o clone para seu computador, ir ao diretório de seu projeto e executar o comando **mvn clean install** no terminal(ter o maven instalado na sua máquina).
